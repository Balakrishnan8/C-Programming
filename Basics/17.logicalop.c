#include"stdio.h"
#include"stdbool.h"

int main()
{
	// Logical operators - and, or, not
	/*
		&&(AND operator)
		||(OR operator)
		!(NOT operator)
	*/
	
	bool x = true;
	bool y = false;
	
	printf("x&&y is %d\n",x&&y);
	printf("x||y is %d\n",x||y);
	printf("!x is %d\n",!x);
	
	
	return 0;
}
