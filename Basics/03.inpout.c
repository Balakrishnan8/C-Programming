#include<stdio.h>

int main()
{
	// printf("format string",argument_list);
	// format string can be %d -integer , %c -character, %s -string(default) , %f -float.
	
	// scanf("format string",argument_list);
	
	// program to get input from user and display it.
	
	int number;
	
	printf("Enter the Number: ");
	scanf("%d",&number);
	
	printf("You Entered : %d\n",number);
	
	
	return 0;
}
